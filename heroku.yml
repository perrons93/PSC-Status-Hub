# Trying to trigger this.....
setup:
  addons:
    - plan: heroku-postgresql
      as: db
build:
  docker:
    frontend: frontend/Dockerfile
#    backend: backend/Dockerfile
run:
  frontend: frontend/entrypoint.sh
#  backend: entrypoint.sh

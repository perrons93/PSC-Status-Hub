# Trying to trigger this.....
setup:
  addons:
    - plan: heroku-postgresql
      as: db
build:
  docker:
    frontend: frontend/Dockerfile
    backend: backend/Dockerfile
run:
  frontend: /bin/bash frontend/entrypoint.sh
  backend: /bin/bash backend/entrypoint.sh

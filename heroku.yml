# Trying to trigger this.....
setup:
  addons:
    - plan: heroku-postgresql
      as: db
build:
  docker:
    frontend: frontend/Dockerfile
    backend: backend/Dockerfile
run:
  frontend: sh frontend/entrypoint.sh
  backend: sh backend/entrypoint.sh
